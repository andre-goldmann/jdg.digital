import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.configcat.app',
  appName: 'configcat',
  webDir: './dist/apps/configcat/browser',
};

export default config;
