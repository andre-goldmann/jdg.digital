import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.angularplayground.app',
  appName: 'angular-playground',
  webDir: '../dist/apps/angular-playground/browser',
};

export default config;
